# Database (overridden by docker-compose.prod.yml for container networking)
DATABASE_URL="postgresql://lynkify:CHANGE_ME@postgres:5432/lynkify_db?schema=public"

# Redis (overridden by docker-compose.prod.yml for container networking)
REDIS_URL="redis://:CHANGE_ME@redis:6379"

# Server
BACKEND_PORT=4000
NODE_ENV=production

# CORS — set to your Vercel domain
FRONTEND_URL=https://lynkify.me

# Docker Compose production vars
POSTGRES_USER=lynkify
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=lynkify_db
REDIS_PASSWORD=CHANGE_ME

# GitHub Container Registry
GITHUB_REPOSITORY=your-username/lynkify-me
